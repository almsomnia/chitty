{
   "name": "chitty",
   "private": true,
   "scripts": {
      "clean:all": "bun --workspaces clean && bunx rimraf node_modules",
      "web:dev": "bun -F web dev",
      "server:dev": "bun -F server dev",
      "server:db": "bun -F server db"
   },
   "devDependencies": {
      "@types/bun": "latest"
   },
   "peerDependencies": {
      "typescript": "^5"
   },
   "workspaces": {
      "packages": [
         "apps/*"
      ]
   }
}
